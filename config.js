/**
 * GitHub Pages Configuration
 * 
 * This file is optional and kept for future extensibility.
 * The site now uses simple relative paths and doesn't require configuration.
 */

window.GITHUB_CONFIG = {
  // Site configuration (optional)
  siteName: 'FIS Bob-a-thon Workshop',
  
  // Add any custom configuration here as needed
};

// Made with Bob
